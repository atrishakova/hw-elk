<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Консольный аппендер с MDC -->
    <appender name="consoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{35} - [%mdc{requestId:-N/A}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Filebeat аппендер с LogstashEncoder для JSON формата -->
    <appender name="filebeatAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/application.log</file>
        <append>true</append>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeCallerData>true</includeCallerData>
            <fieldNames>
                <timestamp>@timestamp</timestamp>
                <message>message</message>
                <level>level</level>
                <logger>logger_name</logger>
                <thread>thread_name</thread>
                <mdc>mdc</mdc>
            </fieldNames>
            <customFields>{"app_name":"demo","app_version":"0.0.1-SNAPSHOT"}</customFields>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>./logs/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Дополнительный JSON аппендер для Logstash -->
    <appender name="logstashAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/logstash.json</file>
        <append>true</append>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeCallerData>true</includeCallerData>
            <includeMdc>true</includeMdc>
            <customFields>{"app_name":"demo","environment":"development"}</customFields>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>./logs/logstash.%d{yyyy-MM-dd}.%i.json.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Логгер для вашего приложения с DEBUG уровнем -->
    <logger name="ru.netology" level="DEBUG" additivity="false">
        <appender-ref ref="consoleAppender"/>
        <appender-ref ref="filebeatAppender"/>
        <appender-ref ref="logstashAppender"/>
    </logger>

    <!-- Корневой логгер -->
    <root level="INFO">
        <appender-ref ref="consoleAppender"/>
        <appender-ref ref="filebeatAppender"/>
        <appender-ref ref="logstashAppender"/>
    </root>
</configuration>